apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
  name: trendify-service
spec:
  type: LoadBalancer
  selector:
    app: trendify
  ports:
    - port: 3000
      targetPort: 80
